@page
@model DevOpsProjectOne.Pages.Plants.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Plant[0].Address)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Plant[0].Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Plant) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Plant_id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Plant_id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Plant_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
