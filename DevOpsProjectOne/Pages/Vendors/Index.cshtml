@page
@model DevOpsProjectOne.Pages.Vendors.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Vendor[0].Vendor_Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vendor[0].Location)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Vendor) {
        <tr>
            <td>
                    @Html.DisplayFor(modelItem => item.Vendor_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Vendor_id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Vendor_id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Vendor_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
